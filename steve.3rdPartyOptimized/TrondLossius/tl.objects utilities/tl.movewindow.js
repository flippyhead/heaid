// tl.movewindow.js//// move a patcher window on-screen//autowatch = 1;// inlets and outletsinlets = 1;outlets = 1;var mypatch;function scan(){	outlet(0, "clear");	mypatch = max.frontpatcher;	outlet(0, "append", mypatch.wind.title);	mypatch = mypatch.wind.next;	while (mypatch) {   		outlet(0, "append", mypatch.title);		mypatch = mypatch.next;	}}function reposition(s){	var x, y;	x = Math.random()*100+50;	y = Math.random()*100+50;	mypatch = max.frontpatcher;	if (mypatch.wind.title == s)	{		post("tl.movewindow: refused to move this window");	}	else	{		mypatch = mypatch.wind.next;		while (mypatch) {			if (mypatch.title == s)			{				mypatch.location = [x, y, x+400, y+400];				break;			}			mypatch = mypatch.next;		}	}	mypatch.bringtofront();}